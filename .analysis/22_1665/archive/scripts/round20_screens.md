# 22-1665 第二十轮：现场探针 8 定标（四链 = R1 / G1 / R2 / G2）——由 `check_round20.py` 生成

## 1. 探针 8 脚索引顺序表（k = R1 区显示的数字）

| k | 脚名 | 端口/位号 | 现场读数（用户回报） |
|---|---|---|---|
| 0 | R1 | PG9 | （未单独回报；由观测①②确认导通：上半屏红） |
| 1 | G1 | PG10 | 背景**绿**色 · 上半屏 |
| 2 | B1 | PG12 | 无反应（背景不亮） |
| 3 | R2 | PG15 | 背景**红**色 · 下半屏 |
| 4 | G2 | PB6 | 背景**绿**色 · 下半屏 |
| 5 | B2 | PB7 | 无反应（背景不亮） |
| 6 | A | PD4 | 无反应（背景不亮） |
| 7 | B | PD5 | 无反应（背景不亮） |
| 8 | C | PD6 | 无反应（背景不亮） |
| 9 | D | PD7 | 无反应（背景不亮） |

## 2. 由读数解出的链表（= 源码默认表）

| 链 | 脚 | 角色 | 区域 (x0,y0,w,h) | 现场依据 |
|---|---|---|---|---|
| C0 | R1 | RED | (0,0,16,8) | 第十二/十七/二十轮三度确认（观测①：红「口」上半屏） |
| C1 | G1 | GREEN | (0,0,16,8) | 探针 8 k=1 → 上半屏绿 |
| C2 | R2 | RED | (0,8,16,8) | 探针 8 k=3 → 下半屏红 |
| C3 | G2 | GREEN | (0,8,16,8) | 探针 8 k=4 → 下半屏绿 |

> B1(k=2) / B2(k=5) / A(k=6) / B(k=7) / C(k=8) / D(k=9) 六根脚现场**全程无反应**⇒ 不与任何链相连，链表一个都不占用。

## 3. 新表预期屏面（`CHAIN_PROBE = 0`，四链全导通）

红色「口」（红链取 R 位：R1 上半 23 点 + R2 下半 29 点 = 52 点）：

```
................
................
................
...###########..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...###########..
...#........##..
................
```

绿色「口」（绿链取 G 位：G1 上半 23 点 + G2 下半 29 点 = 52 点）：

```
................
................
................
...###########..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...###########..
...#........##..
................
```

各链自己的 128 位（红「口」内容；区域局部视图）：


### C0（R1，RED，region=(0,0,16,8)）

```
................
................
................
...###########..
...#........##..
...#........##..
...#........##..
...#........##..
```


### C1（G1，GREEN，region=(0,0,16,8)）

```
................
................
................
................
................
................
................
................
```


### C2（R2，RED，region=(0,8,16,8)）

```
...#........##..
...#........##..
...#........##..
...#........##..
...#........##..
...###########..
...#........##..
................
```


### C3（G2，GREEN，region=(0,8,16,8)）

```
................
................
................
................
................
................
................
................
```


## 4. 三条现场观测的复算

- **观测①（`CHAIN_PROBE=0` 红「口」只在上半屏）**：旧表（R1/A/B/C）下红内容只喂链 0（R1，真链）与链 2（B，无链）⇒ 上半屏 23 点、y≥8 全黑 —— 逐点与实测一致。

- **观测②（`CHAIN_PROBE=5` 上半屏红全亮、绿不亮）**：多链模式的探针 5/6 状态位只有 bit0 ⇒ `st_bits` = 链 0（R1）的 line_mask ⇒ **只拉高 R1 一根脚**（与链表其余行、与各脚是否导通都无关）。「十脚恒定全 1」只在兼容模式下成立。

- **观测③（`CHAIN_PROBE=8` 逐脚读数）**：见 §1 表 —— 探针 8 的参与脚 = `DATA_LINES | R1`、状态位由被测脚序号直接给出 ⇒ **绕过链表逐脚驱动**，因此能看到 G1/R2/G2 三条此前「送内容无反应」的链。


## 5. 下一步现场复验清单（`CHAIN_PROBE = 0` 默认口径，零改宏）

1. **红「口」是否整幅 16 行完整出现？**（上半 R1 + 下半 R2）—— 预期 = 上面 §3 的图；

2. **把默认画面颜色改成绿**（`app_default_display.c`）后，是否出现整幅绿色「口」？（G1 上半 + G2 下半；红链取 R 位 = 0 ⇒ 不自亮）

3. **下半屏的字形是否正常 / 上下颠倒 / 左右镜像 / 有缺笔画？**—— 若颠倒/镜像 ⇒ 给链 2/链 3 加 `flags`（最常见 `_22_1665_XF_ROT180`）再烧一次；

4. **RTT 核对**：`[22_1665] chain0..3` 应打 `lines=0x001/0x002/0x008/0x010`、`role=RED/GREEN/RED/GREEN`、`region=(0,0,16,8)/(0,0,16,8)/(0,8,16,8)/(0,8,16,8)`、`xf=0x0`；`out_lines=0x01B ports=2`；逐脚自检 4 行 R1/G1/R2/G2 全 PASS。


## 6. 仍需现场回答的问题

- **探针 8 时 k=0（R1）的读数**有没有？（用户只报了 k=1..9；R1 的导通性已由观测①②确认，这条只是补全读数表）

- **探针 8 每根脚那 3 秒里，「前 1.5s 2 Hz 闪 / 后 1.5s 细密纹理」两段是否都看到了？**—— 判断「真被驱动」与「只跟电平（悬空/弱耦合）」的关键（本次读数是「背景亮/不亮」，尚不能区分这两者）。
