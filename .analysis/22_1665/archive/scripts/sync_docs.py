#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""第十二轮文档同步：把 22-1665 的旧推定模型描述替换为反解模型（指向 doc/01 §15）。

只做**精确子串替换**（命中即替换、没命中就报告，不猜不误伤）。
"""
import pathlib

ROOT = pathlib.Path("/home/yystation/Program/3833024/Project-STD-main")

REPL = []

# ---------------------------------------------------------------- CLAUDE.md
CLAUDE = ROOT / "doc/CLAUDE.md"
REPL.append((
    CLAUDE,
    "**`22_1665`（16×16 红绿双色 / 默认静态扫描 / 单链 MBI5034B 512 位 / **物理只有 R/G、蓝通道＝点不亮**；2026-09-16 新增、2026-09-17 第十一轮加固：校准开关归一（`CHAIN_BLK_W` 块形状 + `CHAIN_SEG_LAYOUT` 段排布）+ 新增体制开关 `SCAN_SEL`（静态/1-N 扫）与 `CHAIN_DUAL`（单链/双链）+ 链序探针 `CHAIN_PROBE`（默认关）+ 宿主验证 `.analysis/22_1665/`；**无实物核对、按假设起草**，见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md` §14）**",
    "**`22_1665`（**2026-09-17 第十二轮＝现场实测反解模型**：屏面 **16 列 × 8 行**单色 LED 栅格（128 点、1 位/像素）/ 静态 / 单链 **128 位 = 8 片 MBI5034B**；四条规则：链长 128 位（512 位里最后移入的 128 位留存）、片内落点 `(dx,dy)=(3-i%4,(i/4+2)%4)`、块栅格 4 列 × 2 行且块列反向、颜色＝非黑即亮（屏上全红）；探针 0..6（4=片锚点 / 5=全链点亮 / 6=全链+全数据线）；旧推定开关（SCAN_SEL / CHAIN_DUAL / SEG_LAYOUT / BLK_W / G_FIRST / LINE_REVERSE / BIT_REVERSE / BLUE_LINK）已删除，全文备份 `.analysis/22_1665/prev_round/`；见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md` **§15**）**",
))
REPL.append((
    CLAUDE,
    "│   22-1665（16×16 默认静态）：链位映射表 `_22_1665_bit[512]` 2048 + pixel_map 256 + hub75_buff 256 + g_bsrr 128 = **2688B**（`DISP=22_1665` 口径；2026-09-16 新增、2026-09-17 第十一轮重采；g_bsrr[2 段][8 色] 8B/项、`_22_1665_bit[]` 4B/项 = 逐链位 {uint16_t dst; uint8_t seg}）",
    "│   22-1665（16×8 默认静态）：链位映射表 `_22_1665_dst[128]` 256 + pixel_map 128 + hub75_buff 128 + g_bsrr[8 色] 64 = **576B**（`DISP=22_1665` 口径；2026-09-16 新增、**2026-09-17 第十二轮重采（实测反解模型）**，见 doc/01 22-1665 记录 §15）",
))
REPL.append((
    CLAUDE,
    "**22-1665 模组实例注册**（16×16 红绿双色 / **静态扫描**（`scan_lines=1`，无行址线；`set_row` 照 p20 静态 1 扫先例提供、`row` 恒 0，与基类判空跳过等价）/ 单根 SDI 串行链 **512 位 = 32 片 MBI5034B**；发送缓存 `hub75_buff` = 1B/像素颜色索引、与 `pixel_map` 逐字节同布局（链位 ↔ (像素, 段) 收进 init 期预计算的逐链位映射表 `_22_1665_bit[512]`）；**物理仅 R/G 两段、蓝分量点不亮**（`_22_1665_BLUE_LINK=0`，用户裁决 2026-09-16）；**校准开关集中在文件内「★ 校准开关」节**（`CHAIN_BLK_W` / `CHAIN_SEG_LAYOUT` / `G_FIRST` / `LINE_REVERSE` / `BIT_REVERSE` / `DATA_ACTIVE_HIGH`）+ **结构开关**（`SCAN_SEL=1/2/4/8/16`、`CHAIN_DUAL=0/1`）+ **链序探针**（`CHAIN_PROBE=0/1/2/3`，默认关；`SPAN/POS/HOLD` 随附）；`make DISP=22_1665` 选编、**EIDE Debug 现编本模组**；CCM 2688B；**假设起草、待实物核对**，见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md`）",
    "**22-1665 模组实例注册**（**实测反解模型（2026-09-17 第十二轮）**：屏面 **16 列 × 8 行**单色 LED 栅格 = 128 点、1 位/像素 / **静态扫描**（`scan_lines=1`，无行址线；`set_row` 照 p20 静态 1 扫先例提供、`row` 恒 0）/ 单根 SDI 串行链 **128 位 = 8 片 MBI5034B**（驱动 512 位里只有最后移入的 128 位留在链上）；发送缓存 `hub75_buff` = 1B/像素颜色索引、与 `pixel_map` 逐字节同布局；链位 → LED 落点由 init 期预计算的 `_22_1665_dst[128]` 表给出（`(dx,dy)=(3-i%4,(i/4+2)%4)`；块栅格 4 列 × 2 行、块列反向）；**颜色＝非黑即亮**（屏上 LED 全红，观测 C 实证）；**链序探针** `CHAIN_PROBE=0..6`（默认关；4=片锚点 / 5=全链点亮 / 6=全链+全数据线；`POS/SPAN/HOLD` 随附）；`make DISP=22_1665` 选编、**EIDE Debug 现编本模组**；CCM **576B**；见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md` §15）",
))
REPL.append((
    CLAUDE,
    "| **`dev_display_22_1665.c`**（`2200001665`，料号推定） | 16×16（1×16 / 1×16，默认单块；`MODULE_ROWS/COLS` 可调，帧长随动） | **默认静态 1 扫**（可切 `SCAN_SEL=2/4/8/16`），**默认 1 数据线**（单链 MBI5034B，帧 512 位 = 32 片；可切 `CHAIN_DUAL=1` R1+G1 双链） | 2688B（链位表 2048 + pixel_map 256 + hub75_buff 256 + g_bsrr 128） | **EIDE Debug 现行编入本文件**（1_263 / 22_1703 均在其 excludeList 内，2026-09-16 用户裁决）；`make DISP=22_1665` 选编；**假设起草、待实物核对**（doc/01 22-1665 记录 §9/§10/§14） |",
    "| **`dev_display_22_1665.c`**（`2200001665`，料号推定） | **16 列 × 8 行**（= 本驱动喂养那条链覆盖的 128 个 LED；1 位/像素；`_22_1665_BLK_COLS` 1/2/4/8 可切换屏面形状 4×32 / 8×16 / 16×8 / 32×4） | **静态 1 扫**，**单链 128 位 = 8 片 MBI5034B**（模组其余 3 条链不接在本驱动 SDI 上；用探针 6 判其数据脚） | **576B**（`_22_1665_dst[128]` 256 + pixel_map 128 + hub75_buff 128 + g_bsrr 64） | **EIDE Debug 现行编入本文件**（1_263 / 22_1703 均在其 excludeList 内，2026-09-16 用户裁决）；`make DISP=22_1665` 选编；**模型由现场两条观测反解、唯一解**（doc/01 22-1665 记录 §15） |",
))
REPL.append((
    CLAUDE,
    "- **22_1665 实测段尺寸（2026-09-17 第十一轮复采：开关归一 + 体制开关 + 链序探针 + 宿主验证；`APP_DIAG_BANNER` 默认开）**",
    "- **22_1665 实测段尺寸（2026-09-17 第十二轮复采：现场两条观测反解出真实接线 → 驱动整体替换；`APP_DIAG_BANNER` 默认开，零告警）**：`PROTO=ALL` `DISP=22_1665` text **166820** / rodata 200896 / data 1672 / **ccmram 8092**（显示模组 **576B**：`_22_1665_dst[128]` 256 + pixel_map 128 + hub75_buff 128 + g_bsrr[8] 64；非显示 7516B）/ bss 126400（SRAM 合计 130632B，余 440B），elf md5 `4fe9a25dd47f5d62fb21369adcfceb20` / hex md5 `8b760f30a9d653abca5ff8fce91c1c30` / bin md5 `f0fdd599619cda7cf3ad36da6695364f` / 内嵌 tree=`203cc954`；模型矩阵编译自检 `.analysis/22_1665/check_model_matrix.sh` **19/19 通过**（探针 0..6 × BLK_COLS 1/2/4/8 × 极性，非法组合被 `_Static_assert` 拦下）；scan 热路径 = **128 链位/帧**（帧时长 ≈25~35µs，TIM3 500µs 周期余量 >10×，待 DWT 复核）。**上一轮（第十一轮）旧记**：" ,
))

# ---------------------------------------------------------------- 构建开关总表.md
SWITCH = ROOT / "doc/构建开关总表.md"
REPL.append((
    SWITCH,
    "`22_1665` = 16x16 红绿双色**静态扫描**单链 MBI5034B（帧 512 位 = 32 片，CCM **768B**，**物理仅 R/G、蓝通道点不亮**（`_22_1665_BLUE_LINK=0`，用户裁决 2026-09-16），**假设起草待实物核对**，见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md`）",
    "`22_1665` = **实测反解模型**（2026-09-17 第十二轮）：**16 列 × 8 行**单色 LED 栅格（128 点、1 位/像素）、**静态扫描**、单链 **128 位 = 8 片 MBI5034B**，CCM **576B**，颜色＝非黑即亮（详见 `doc/01_显示系统/22-1665模组驱动分析与迁移记录.md` **§15**）",
))

hit = miss = 0
for path, old, new in REPL:
    text = path.read_text(encoding="utf-8")
    if old in text:
        path.write_text(text.replace(old, new, 1), encoding="utf-8")
        print(f"[OK]   {path.name}: 命中 1 处")
        hit += 1
    else:
        print(f"[MISS] {path.name}: 未命中 → {old[:60]}…")
        miss += 1
print(f"\n命中 {hit} / 未命中 {miss}")
